apiVersion: v1
kind: Service
metadata:
  name: productservice
  labels:
    app: productservice
spec:
  type: LoadBalancer
  selector:
    app: productservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3200